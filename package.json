{
  "name": "degrees",
  "version": "0.1.0",
  "description": "Spec-first portable degree profiles for agent skills and tools.",
  "type": "module",
  "bin": {
    "degrees": "scripts/degrees.mjs"
  },
  "scripts": {
    "resolve": "node scripts/resolve-degree.mjs",
    "validate": "node scripts/validate-degrees.mjs",
    "check:fixtures": "node scripts/check-resolver-fixtures.mjs"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
